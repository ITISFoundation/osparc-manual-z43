#
# SEE https://gitlab.com/pages/plain-html
#
pages:
  stage: deploy
  tags:
    - ubuntu_DOCKER
  script:
    - mkdir .public
    - cp -r * .public
    - mv .public public
  artifacts:
    paths:
      - public
  only:
    - master
