image: alpine:latest

pages:
  stage: deploy
  tags:
    - DOCKER_modern
  script:
    - cp -r docs .public
    - mv .public public
    - ls -la public
  artifacts:
    paths:
      - public
  only:
    - master
